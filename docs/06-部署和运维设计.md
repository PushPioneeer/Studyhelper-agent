# 学习助手 - 部署和运维设计

## 1. 部署架构

### 1.1 初期部署（单机）

适用于开发环境和小规模测试（<1000 用户）

```yaml
# docker-compose.yml
version: '3.8'

services:
  # Nginx 反向代理
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - bff
      - backend
    networks:
      - app-network

  # BFF 层 (Node.js + Fastify)
  bff:
    build:
      context: ./bff
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
      - PORT=3000
      - BACKEND_URL=http://backend:8000
      - REDIS_HOST=redis
      - JWT_SECRET=${JWT_SECRET}
    depends_on:
      - redis
    networks:
      - app-network
    restart: unless-stopped

  # 后端服务 (FastAPI)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      - ENV=production
      - DATABASE_URL=postgresql://user:pass@postgres:5432/study_helper
      - REDIS_HOST=redis
      - GUIJI_API_KEY=${GUIJI_API_KEY}
      - STORAGE_PATH=/app/storage
    volumes:
      - ./storage:/app/storage
    depends_on:
      - postgres
      - redis
    networks:
      - app-network
    restart: unless-stopped

  # PostgreSQL 数据库
  postgres:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass
      - POSTGRES_DB=study_helper
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./backups:/backups
    networks:
      - app-network
    restart: unless-stopped

  # Redis 缓存
  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data
    networks:
      - app-network
    restart: unless-stopped

  # Celery Worker（异步任务）
  celery-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: celery -A app.tasks worker --loglevel=info
    environment:
      - DATABASE_URL=postgresql://user:pass@postgres:5432/study_helper
      - REDIS_HOST=redis
    volumes:
      - ./storage:/app/storage
    depends_on:
      - postgres
      - redis
    networks:
      - app-network
    restart: unless-stopped

  # Celery Beat（定时任务）
  celery-beat:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: celery -A app.tasks beat --loglevel=info
    environment:
      - DATABASE_URL=postgresql://user:pass@postgres:5432/study_helper
      - REDIS_HOST=redis
    depends_on:
      - redis
    networks:
      - app-network
    restart: unless-stopped

volumes:
  postgres-data:
  redis-data:

networks:
  app-network:
    driver: bridge
```

---

### 1.2 中期部署（多机）

适用于生产环境（1000-10000 用户）

```
┌─────────────────────┐
│   负载均衡器         │
│   (Nginx/SLB)       │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│  BFF 实例 1          │
│  BFF 实例 2          │
│  BFF 实例 3          │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│  Backend 实例 1      │
│  Backend 实例 2      │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│  PostgreSQL 主库     │
│  PostgreSQL 从库     │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│  Redis Sentinel     │
│  (3 节点集群)        │
└─────────────────────┘
```

---

## 2. 环境配置

### 2.1 环境变量

```bash
# .env.production

# 应用配置
NODE_ENV=production
ENV=production
APP_NAME=study-helper
APP_VERSION=1.0.0

# 数据库配置
DATABASE_URL=postgresql://user:password@localhost:5432/study_helper
DB_HOST=localhost
DB_PORT=5432
DB_NAME=study_helper
DB_USER=user
DB_PASSWORD=password

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis_password

# JWT 配置
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d
REFRESH_TOKEN_SECRET=your-refresh-token-secret
REFRESH_TOKEN_EXPIRES_IN=30d

# 硅基流动 API 配置（重要！）
GUIJI_API_KEY=sk-wgffeealqezrbzdcycwqimxffvhypuqjzqfirmdpndmdcstl
GUIJI_API_BASE=https://api.siliconflow.cn/v1
GUIJI_MODEL=Qwen/Qwen2.5-VL-72B-Instruct
GUIJI_TEMPERATURE=0.7
GUIJI_MAX_TOKENS=2048

# 文件存储
STORAGE_PATH=/var/www/study-helper/storage
MAX_FILE_SIZE_MB=50
MAX_IMAGE_SIZE_MB=10

# 限流配置
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# 日志配置
LOG_LEVEL=info
LOG_FILE_PATH=/var/log/study-helper

# 监控配置
SENTRY_DSN=https://xxx@sentry.io/xxx
```

**环境变量说明**
```bash
# 硅基流动 API 配置说明
GUIJI_API_KEY=sk-wgffeealqezrbzdcycwqimxffvhypuqjzqfirmdpndmdcstl  # 你的 API Key
GUIJI_API_BASE=https://api.siliconflow.cn/v1  # API 基础 URL
GUIJI_MODEL=Qwen/Qwen2.5-VL-72B-Instruct  # 视觉语言模型
GUIJI_TEMPERATURE=0.7  # 模型温度（0.0-1.0，越高越有创意）
GUIJI_MAX_TOKENS=2048  # 最大输出 token 数
```

---

### 2.2 Nginx 配置

```nginx
# nginx/nginx.conf
user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
    use epoll;
    multi_accept on;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for" '
                    'rt=$request_time uct="$upstream_connect_time" '
                    'uht="$upstream_header_time" urt="$upstream_response_time"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # 限流
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
    limit_conn_zone $binary_remote_addr zone=conn_limit:10m;

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml application/json application/javascript 
               application/xml application/xml+rss text/javascript;

    # BFF 服务
    upstream bff_servers {
        least_conn;
        server bff:3000 max_fails=3 fail_timeout=30s;
        server bff:3001 max_fails=3 fail_timeout=30s;
        server bff:3002 max_fails=3 fail_timeout=30s;
    }

    # 后端服务
    upstream backend_servers {
        least_conn;
        server backend:8000 max_fails=3 fail_timeout=30s;
        server backend:8001 max_fails=3 fail_timeout=30s;
    }

    server {
        listen 80;
        server_name api.studyhelper.com;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl http2;
        server_name api.studyhelper.com;

        ssl_certificate /etc/nginx/ssl/fullchain.pem;
        ssl_certificate_key /etc/nginx/ssl/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers on;
        ssl_session_cache shared:SSL:10m;
        ssl_session_timeout 10m;

        # 安全头
        add_header Strict-Transport-Security "max-age=31536000" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;

        # API 限流
        location /api/ {
            limit_req zone=api_limit burst=20 nodelay;
            limit_conn conn_limit 10;

            proxy_pass http://bff_servers;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
            proxy_read_timeout 60s;
            proxy_connect_timeout 60s;
        }

        # 静态文件
        location /static/ {
            alias /var/www/study-helper/static/;
            expires 30d;
            add_header Cache-Control "public, immutable";
        }

        # 上传文件
        location /uploads/ {
            alias /var/www/study-helper/storage/;
            expires 7d;
            add_header Cache-Control "public";
        }

        # 健康检查
        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }

        # 错误页面
        error_page 500 502 503 504 /50x.html;
        location = /50x.html {
            root /usr/share/nginx/html;
        }
    }
}
```

---

## 3. Docker 配置

### 3.1 BFF Dockerfile

```dockerfile
# bff/Dockerfile
FROM node:18-alpine AS builder

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production

COPY . .
RUN npm run build

FROM node:18-alpine

WORKDIR /app

COPY --from=builder /app/node_modules ./node_modules
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/package.json ./

ENV NODE_ENV=production

EXPOSE 3000

CMD ["node", "dist/index.js"]
```

### 3.2 Backend Dockerfile

```dockerfile
# backend/Dockerfile
FROM python:3.11-slim

WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    gcc \
    postgresql-client \
    && rm -rf /var/lib/apt/lists/*

# 安装 Python 依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV PYTHONPATH=/app
ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

---

## 4. 数据库迁移

### 4.1 迁移脚本

```bash
#!/bin/bash
# scripts/migrate.sh

set -e

echo "Running database migrations..."

cd backend

# 使用 Alembic 进行迁移
alembic upgrade head

echo "Migrations completed!"
```

### 4.2 数据备份

```bash
#!/bin/bash
# scripts/backup.sh

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/backups/study_helper"
DB_NAME="study_helper"
DB_USER="postgres"

mkdir -p $BACKUP_DIR

echo "Starting backup at $DATE..."

# 全量备份
pg_dump -U $DB_USER -Fc $DB_NAME > $BACKUP_DIR/full_$DATE.dump

# 压缩备份
gzip $BACKUP_DIR/full_$DATE.dump

# 删除 7 天前的备份
find $BACKUP_DIR -name "*.dump.gz" -mtime +7 -delete

echo "Backup completed: $BACKUP_DIR/full_$DATE.dump.gz"
```

---

## 5. 监控和日志

### 5.1 应用监控

**Prometheus + Grafana 配置**

```yaml
# prometheus.yml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'nginx'
    static_configs:
      - targets: ['nginx:9113']

  - job_name: 'postgres'
    static_configs:
      - targets: ['postgres-exporter:9187']

  - job_name: 'redis'
    static_configs:
      - targets: ['redis-exporter:9121']

  - job_name: 'backend'
    static_configs:
      - targets: ['backend:8000']
    metrics_path: '/metrics'
```

### 5.2 日志收集

**ELK Stack 配置**

```yaml
# docker-compose.monitoring.yml
version: '3.8'

services:
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
    volumes:
      - elasticsearch-data:/usr/share/elasticsearch/data
    ports:
      - "9200:9200"

  logstash:
    image: docker.elastic.co/logstash/logstash:8.11.0
    volumes:
      - ./logstash/pipeline:/usr/share/logstash/pipeline
    depends_on:
      - elasticsearch

  kibana:
    image: docker.elastic.co/kibana/kibana:8.11.0
    ports:
      - "5601:5601"
    depends_on:
      - elasticsearch

  filebeat:
    image: docker.elastic.co/beats/filebeat:8.11.0
    volumes:
      - ./filebeat.yml:/usr/share/filebeat/filebeat.yml:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    depends_on:
      - elasticsearch

volumes:
  elasticsearch-data:
```

### 5.3 日志配置

```python
# backend/app/core/logging_config.py
import logging
from logging.handlers import RotatingFileHandler
import os

def setup_logging():
    log_dir = os.getenv('LOG_FILE_PATH', '/var/log/study-helper')
    os.makedirs(log_dir, exist_ok=True)
    
    # 创建日志处理器
    file_handler = RotatingFileHandler(
        f'{log_dir}/app.log',
        maxBytes=10*1024*1024,  # 10MB
        backupCount=10,
        encoding='utf-8'
    )
    
    console_handler = logging.StreamHandler()
    
    # 设置格式
    formatter = logging.Formatter(
        '%(asctime)s - %(name)s - %(levelname)s - [%(filename)s:%(lineno)d] - %(message)s'
    )
    
    file_handler.setFormatter(formatter)
    console_handler.setFormatter(formatter)
    
    # 设置日志级别
    file_handler.setLevel(logging.INFO)
    console_handler.setLevel(logging.INFO)
    
    # 配置根日志记录器
    root_logger = logging.getLogger()
    root_logger.setLevel(logging.INFO)
    root_logger.addHandler(file_handler)
    root_logger.addHandler(console_handler)
```

---

## 6. CI/CD 配置

### 6.1 GitHub Actions

```yaml
# .github/workflows/ci.yml
name: CI/CD

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
      
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install backend dependencies
        working-directory: ./backend
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-cov
      
      - name: Install BFF dependencies
        working-directory: ./bff
        run: npm ci
      
      - name: Run backend tests
        working-directory: ./backend
        run: pytest --cov=app tests/
      
      - name: Run BFF tests
        working-directory: ./bff
        run: npm test
      
      - name: Upload coverage
        uses: codecov/codecov-action@v3

  build:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Build and push BFF
        uses: docker/build-push-action@v4
        with:
          context: ./bff
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/study-helper-bff:latest
      
      - name: Build and push Backend
        uses: docker/build-push-action@v4
        with:
          context: ./backend
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/study-helper-backend:latest

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Deploy to server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_KEY }}
          script: |
            cd /opt/study-helper
            docker-compose pull
            docker-compose up -d
            docker system prune -f
```

---

## 7. 运维脚本

### 7.1 健康检查

```bash
#!/bin/bash
# scripts/health-check.sh

set -e

echo "Running health checks..."

# 检查 Nginx
if curl -f http://localhost/health > /dev/null 2>&1; then
    echo "✓ Nginx is healthy"
else
    echo "✗ Nginx is unhealthy"
    exit 1
fi

# 检查 BFF
if curl -f http://localhost:3000/health > /dev/null 2>&1; then
    echo "✓ BFF is healthy"
else
    echo "✗ BFF is unhealthy"
    exit 1
fi

# 检查 Backend
if curl -f http://localhost:8000/health > /dev/null 2>&1; then
    echo "✓ Backend is healthy"
else
    echo "✗ Backend is unhealthy"
    exit 1
fi

# 检查 PostgreSQL
if pg_isready -h localhost -U postgres > /dev/null 2>&1; then
    echo "✓ PostgreSQL is healthy"
else
    echo "✗ PostgreSQL is unhealthy"
    exit 1
fi

# 检查 Redis
if redis-cli ping > /dev/null 2>&1; then
    echo "✓ Redis is healthy"
else
    echo "✗ Redis is unhealthy"
    exit 1
fi

echo "All health checks passed!"
```

### 7.2 日志清理

```bash
#!/bin/bash
# scripts/cleanup-logs.sh

LOG_DIR="/var/log/study-helper"
MAX_AGE_DAYS=30
MAX_SIZE_MB=100

echo "Cleaning up logs..."

# 删除 30 天前的日志
find $LOG_DIR -name "*.log.*" -mtime +$MAX_AGE_DAYS -delete

# 压缩当前日志
find $LOG_DIR -name "*.log" -size +${MAX_SIZE_MB}M -exec gzip {} \;

echo "Log cleanup completed!"
```

### 7.3 数据库清理

```bash
#!/bin/bash
# scripts/cleanup-database.sh

psql -U postgres -d study_helper << EOF

-- 清理过期验证码
DELETE FROM verification_codes 
WHERE expires_at < CURRENT_TIMESTAMP - INTERVAL '1 day';

-- 清理过期会话
DELETE FROM user_sessions 
WHERE expires_at < CURRENT_TIMESTAMP;

-- 清理 API 日志（保留 30 天）
DELETE FROM api_logs 
WHERE created_at < CURRENT_TIMESTAMP - INTERVAL '30 days';

-- 清理软删除的数据（保留 90 天）
DELETE FROM users WHERE deleted_at < CURRENT_TIMESTAMP - INTERVAL '90 days';
DELETE FROM questions WHERE deleted_at < CURRENT_TIMESTAMP - INTERVAL '90 days';
DELETE FROM coursewares WHERE deleted_at < CURRENT_TIMESTAMP - INTERVAL '90 days';
DELETE FROM mistakes WHERE deleted_at < CURRENT_TIMESTAMP - INTERVAL '90 days';

EOF

echo "Database cleanup completed!"
```

---

## 8. 故障处理

### 8.1 常见问题处理

**问题 1：数据库连接数过多**

```bash
# 查看当前连接
psql -U postgres -c "SELECT count(*) FROM pg_stat_activity;"

# 查看连接详情
psql -U postgres -c "SELECT * FROM pg_stat_activity WHERE state = 'active';"

# 杀掉空闲连接
psql -U postgres -c "
SELECT pg_terminate_backend(pid) 
FROM pg_stat_activity 
WHERE state = 'idle' 
  AND query_start < CURRENT_TIMESTAMP - INTERVAL '1 hour';
"
```

**问题 2：Redis 内存不足**

```bash
# 查看内存使用
redis-cli INFO memory

# 查看大 key
redis-cli --bigkeys

# 清理过期 key
redis-cli MEMORY PURGE

# 手动驱逐 key
redis-cli MEMORY DOCTOR
```

**问题 3：服务响应慢**

```bash
# 查看慢查询
psql -U postgres -d study_helper -c "
SELECT query, calls, total_time, mean_time 
FROM pg_stat_statements 
ORDER BY total_time DESC 
LIMIT 10;
"

# 查看应用日志
tail -f /var/log/study-helper/app.log | grep -E "ERROR|WARN"

# 查看系统负载
top
htop
iostat -x 1
```

### 8.2 告警配置

```yaml
# alertmanager/alertmanager.yml
global:
  smtp_smarthost: 'smtp.gmail.com:587'
  smtp_from: 'alerts@studyhelper.com'

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'email-notifications'

receivers:
  - name: 'email-notifications'
    email_configs:
      - to: 'admin@studyhelper.com'
        send_resolved: true

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'instance']
```

---

## 9. 性能优化

### 9.1 数据库优化

```sql
-- 分析慢查询
EXPLAIN (ANALYZE, BUFFERS) 
SELECT * FROM questions 
WHERE user_id = 'xxx' 
  AND created_at > '2026-01-01';

-- 添加索引
CREATE INDEX CONCURRENTLY idx_questions_user_created 
ON questions(user_id, created_at DESC);

-- 更新统计信息
ANALYZE questions;
ANALYZE mistakes;
ANALYZE coursewares;

-- 检查索引使用情况
SELECT schemaname, tablename, indexname, idx_scan 
FROM pg_stat_user_indexes 
ORDER BY idx_scan;
```

### 9.2 缓存优化

```python
# 缓存热点数据
@cache.cached(timeout=3600, key_prefix='hot_knowledge_points')
async def get_hot_knowledge_points():
    return await db.query(KnowledgePoint).limit(100).all()

# 缓存用户信息
@cache.cached(timeout=1800, key_prefix='user_info')
async def get_user_info(user_id: str):
    return await db.query(User).filter(User.id == user_id).first()
```

---

## 10. 安全加固

### 10.1 系统安全

```bash
# 防火墙配置
ufw default deny incoming
ufw default allow outgoing
ufw allow ssh
ufw allow http
ufw allow https
ufw enable

# 禁用 root 登录
sed -i 's/PermitRootLogin yes/PermitRootLogin no/' /etc/ssh/sshd_config
systemctl restart sshd

# 配置 fail2ban
cat > /etc/fail2ban/jail.local << EOF
[DEFAULT]
bantime = 3600
findtime = 600
maxretry = 5

[sshd]
enabled = true

[nginx-http-auth]
enabled = true
EOF

systemctl enable fail2ban
systemctl start fail2ban
```

### 10.2 应用安全

```python
# 安全头配置
@app.middleware("http")
async def add_security_headers(request: Request, call_next):
    response = await call_next(request)
    response.headers["X-Content-Type-Options"] = "nosniff"
    response.headers["X-Frame-Options"] = "DENY"
    response.headers["X-XSS-Protection"] = "1; mode=block"
    response.headers["Strict-Transport-Security"] = "max-age=31536000"
    return response
```

---

## 11. 文档修订记录

| 版本 | 日期 | 修订内容 | 修订人 |
|-----|------|---------|--------|
| v1.0 | 2026-03-17 | 初始版本 | - |

---

## 12. 设计文档完成总结

✅ **已完成的设计文档：**

1. ✅ 需求分析文档
2. ✅ 系统架构设计
3. ✅ 数据库设计
4. ✅ API 接口设计
5. ✅ 前端设计
6. ✅ 部署和运维设计（本文档）

📋 **所有设计文档已完成，可以开始实现阶段！**

---

## 13. 移动端 Agent 特性总结

### 13.1 Agent 核心能力

**解题 Agent**
- ✅ 使用 `create_agent()` 创建（LangChain 最佳实践）
- ✅ 配备 `MemorySaver` 持久化会话状态
- ✅ 支持视觉能力（Qwen/Qwen2.5-VL-72B-Instruct）
- ✅ 4 个专用工具：题目识别、解题、知识点提取、类似题生成
- ✅ System Prompt 明确移动端学习助手定位
- ✅ 会话配置：`thread_id = user_id:question_id`

**课件解析 Agent**
- ✅ 使用 `create_agent()` 创建
- ✅ 支持 PDF/PPT/Word/Markdown 四种格式
- ✅ 生成详细笔记（非大纲式）
- ✅ System Prompt 强调排版精美、便于移动端阅读

**追问功能（对话式 Agent）**
- ✅ 基于会话历史的连续对话
- ✅ 硬性限制：每道题目最多 10 次追问
- ✅ 防无限循环：`recursion_limit = 15`
- ✅ 完整的对话记录存储

### 13.2 移动端优化

**性能优化**
- ✅ React Native + Expo（跨平台）
- ✅ FlashList 虚拟化列表（比 FlatList 快 3-5 倍）
- ✅ expo-image 图片缓存（自动 WebP 支持）
- ✅ Zustand 状态管理（轻量、无需 Provider）
- ✅ Native Stack 导航（原生性能）

**网络优化**
- ✅ 图片前端压缩（10MB → 2MB）
- ✅ 请求防抖和节流
- ✅ 离线缓存策略
- ✅ 轮询机制（Agent 异步处理）

**用户体验**
- ✅ 骨架屏加载
- ✅ 上传进度显示
- ✅ 追问次数实时提示
- ✅ 验证码键盘、自动读取短信
- ✅ 安全的 Token 存储（SecureStore）

**安全加固**
- ✅ HTTPS/TLS 1.3 传输加密
- ✅ 本地数据加密存储
- ✅ API 限流防刷
- ✅ 文件类型校验
- ✅ Root/越狱检测（可选）

### 13.3 技术栈锁定

**前端（移动端）**
```
React Native (Expo)
├── TypeScript
├── Zustand（状态管理）
├── React Navigation（导航）
├── FlashList（列表）
├── expo-image（图片）
├── Reanimated（动画）
└── Axios（HTTP）
```

**后端**
```
FastAPI (Python)
├── LangChain（Agent 框架）
├── SQLAlchemy（ORM）
├── Redis（缓存/队列）
├── Celery（异步任务）
└── 文件解析库（pdf-parse, python-pptx, python-docx）
```

**BFF 层**
```
Node.js (Fastify)
├── TypeScript
├── JWT 认证
├── Redis 限流
└── 请求聚合
```

**数据库**
```
PostgreSQL 15+
├── 用户数据
├── 题目记录
├── 错题集
├── 课件笔记
└── 对话历史
```

**AI 模型**
```
硅基流动 - Qwen/Qwen2.5-VL-72B-Instruct
├── 视觉能力（OCR + 题目理解）
├── 解题能力
├── 知识点提取
└── 对话能力
```

---

## 14. 实现阶段路线图

### 第一阶段：基础架构（1-2 周）
- [ ] 初始化 React Native 项目（Expo）
- [ ] 初始化 FastAPI 后端项目
- [ ] 初始化 Node.js BFF 项目
- [ ] 配置 PostgreSQL 数据库
- [ ] 配置 Redis 缓存
- [ ] 搭建 Docker 开发环境

### 第二阶段：用户系统（1 周）
- [ ] 实现手机号登录/注册
- [ ] 实现 JWT Token 认证
- [ ] 实现用户信息管理
- [ ] 移动端登录界面

### 第三阶段：解题功能（2-3 周）
- [ ] 实现图片上传接口
- [ ] 集成硅基流动 API
- [ ] 开发解题 Agent（create_agent + tools）
- [ ] 实现追问功能（对话式 Agent）
- [ ] 移动端拍照解题界面
- [ ] 追问聊天界面

### 第四阶段：课件解析（2 周）
- [ ] 实现文件上传接口
- [ ] 开发课件解析 Agent
- [ ] 实现笔记生成功能
- [ ] 移动端课件上传和笔记查看

### 第五阶段：错题集（1 周）
- [ ] 实现错题收录
- [ ] 实现错题管理（CRUD）
- [ ] 实现批量操作
- [ ] 移动端错题集界面

### 第六阶段：统计和优化（1 周）
- [ ] 实现学习统计
- [ ] 性能优化（缓存、索引）
- [ ] 集成测试
- [ ] 移动端统计页面

### 第七阶段：部署上线（1 周）
- [ ] 配置生产环境
- [ ] 部署 Docker 容器
- [ ] 配置 Nginx 反向代理
- [ ] 配置监控和日志
- [ ] 移动端打包发布

**总计：约 9-11 周完成 MVP 版本**

---

🎉 **设计阶段完成，准备开始实现！**
